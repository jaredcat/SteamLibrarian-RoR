<div class="container-fluid" style="margin-top:20px;">
  <div class="row">
		<div class="col-xs-2" style="min-width: 150px">
	      <h3 class="text-left">Filters</h3>
	      <div class="well well-sm">
	      	<%= render 'filters.html.erb' %>
	      </div>
		</div>
		<div class="col-xs-9">
			<h3 class="text-left">Games (<%= @gamelist.length %>)</h3>
			<div class="well">
				<% if @gamelist.length < 1 %>
					<h2>No games returned. Select less filters</h2>
				<% else %>
					<div class="btn-row" style="padding-bottom: 10px; margin-top: -5px;"><button type="button" class="random-button btn btn-default pull-right">Random</button></div>
					<table id="gametable" class="table table-striped table-bordered" cellspacing="0" width="100%">
						<thead>
							<tr>
								<th>Image</th>
								<th>Name</th>
								<th>Time Played</th>
								<th>Genres</th>
								<th>Themes</th>
								<th>Concepts</th>
								<th>Launch Game</th>
							</tr>
						</thead>
						<tbody>
							<% @gamelist.each do |game| %>
	  							<tr>
	  								<td><img src="<%= game.image %>" /> </td>
	  								<td class="game-name"><%=link_to game.name, game.site_detail_url, target: "_blank" %></td>
	  								<td class="playtime_forever"><%= UsersGame.find_by(user_id: @user.id, game_id: game.id).playtime_forever %></td>
	  								<td>
	  									<% @genres.each do |genre| 
	  										if genre.game_id == game.id %>
	  											<%= genre.genre %>, 
	  										<% end %>
	  									<% end %>
	  								</td>
	  								<td>
	  									<% @themes.each do |theme| 
	  										if theme.game_id == game.id %>
	  											<%= theme.theme %>, 
	  										<% end %>
	  									<% end %>
	  								</td>
	  								<td>
	  									<% @concepts.each do |concept| 
	  										if concept.game_id == game.id %>
	  											<%= concept.concept %>, 
	  										<% end %>
	  									<% end %>
	  								</td>
	  								<td><a href="steam://run/<%= game.appid %>" style="color:white"><button type="button" role="button" class="btn btn-primary btn-lg btn-block">Run</button></a></td>
	  							</tr>
							<% end %>
						</tbody>
					</table>
					<div class="btn-row" style="padding-top: 10px; margin-bottom: -5px;">
						<button type="button" class="random-button btn btn-default pull-right">Random</button>
					</div>
				<% end %>
			</div>
		</div>
	</div>
</div>